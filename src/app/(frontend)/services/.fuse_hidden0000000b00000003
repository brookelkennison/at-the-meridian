import Link from 'next/link'
import Reveal from '@/components/Reveal'
import ParallaxBackdrop from '@/components/ParallaxBackdrop'
import { images } from '@/lib/images'

export const metadata = {
  title: 'Services — At The Meridian',
  description:
    'One studio for the entire revenue stack: conversion-engineered websites, SEO, paid acquisition, CRM, automation, and lead generation.',
}

const services = [
  {
    num: '01',
    name: 'Website Builds',
    tag: 'Project',
    outcome:
      'A conversion-engineered site that turns traffic into pipeline — hand-coded, fast, and built around your buyer’s actual decision path.',
    includes: [
      'Strategy, copy, design & development',
      'Conversion-focused UX and funnels',
      'On-page & technical SEO architecture',
      'Analytics & conversion tracking',
    ],
  },
  {
    num: '02',
    name: 'SEO & Authority',
    tag: 'Retainer',
    outcome:
      'Compounding organic reach that takes the top of local search — technical foundations, content velocity, and authority that builds month after month.',
    includes: [
      'Technical + on-page optimization',
      'Keyword research & content planning',
      'SEO content & blog publishing',
      'Rank tracking + monthly reporting',
    ],
  },
  {
    num: '03',
    name: 'Paid Acquisition',
    tag: 'Retainer',
    outcome:
      'Profitable Google & Meta programs run with discipline — small, deliberate tests first, then scale what converts. Optimized toward leads, not clicks.',
    includes: [
      'Foundation-first: site converts before we scale spend',
      'Google Ads + Meta management',
      'Ad spend billed direct to your account',
      '30-day learn-and-iterate framework',
    ],
  },
  {
    num: '04',
    name: 'CRM & Automation',
    tag: 'Retainer',
    outcome:
      'The plumbing behind the growth — systems that capture, nurture, score, and route every dollar of demand so nothing slips through.',
    includes: [
      'CRM buildout & pipeline automation',
      'Lead scoring & routing',
      'Email / SMS sequences',
      'Long-cycle nurture for slow decisions',
    ],
  },
  {
    num: '05',
    name: 'Lead Generation',
    tag: 'Retainer',
    outcome:
      'Qualified conversations on your calendar, week after week — landing pages, funnels, and CRO that turn demand into booked, ready buyers.',
    includes: [
      'Dedicated lead-gen landing pages',
      'Funnel & conversion-rate optimization',
      'CTA testing & offer refinement',
      'Intake that pre-qualifies before the call',
    ],
  },
]

export default function ServicesPage() {
  return (
    <div className="bg-bg text-ink font-sans font-light antialiased">
      {/* ─────────── Hero ─────────── */}
      <header className="relative pt-36 pb-20 lg:pt-44 lg:pb-28 overflow-hidden">
        <div
          aria-hidden
          className="pointer-events-none absolute top-1/3 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[140vmin] h-[140vmin]"
          style={{
            background:
              'radial-gradient(circle, rgba(125, 211, 252, 0.12) 0%, transparent 50%)',
          }}
        />
        <div className="max-w-site mx-auto px-5 sm:px-10 lg:px-20 relative z-10">
          <span className="eyebrow-dark">Capabilities</span>
          <h1 className="font-serif font-light tracking-tight leading-[0.98] text-[44px] sm:text-7xl lg:text-8xl mt-7 mb-8 max-w-[16ch]">
            One studio. <em className="hero-italic-grad font-light">Every lever that matters.</em>
          </h1>
          <p className="text-ink-dim text-base sm:text-lg max-w-[58ch] leading-relaxed">
            Most agencies do one thing well and outsource the rest. We built At The Meridian to own the entire revenue stack — so nothing breaks at the seams.
          </p>
        </div>
        <div className="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/40 to-transparent" />
      </header>

      {/* ─────────── Service list ─────────── */}
      <section className="border-t border-line py-20 lg:py-28">
        <div className="max-w-site mx-auto px-5 sm:px-10 lg:px-20">
          <div className="grid lg:grid-cols-2 gap-6 lg:gap-8">
            {services.map((s, i) => (
              <Reveal
                key={s.num}
                delay={(i % 2) * 80}
                className="surface-glow border border-line rounded-2xl p-8 lg:p-10 hover:border-accent/30 transition-all hover:-translate-y-1 flex flex-col"
              >
                <div className="flex items-start justify-between mb-6">
                  <span className="font-serif italic text-2xl text-accent">{s.num}</span>
                  <span className="text-[10px] tracking-[0.22em] uppercase text-accent border border-accent/40 rounded-full px-3 py-1.5">
                    {s.tag}
                  </span>
                </div>
                <h2 className="font-serif font-light text-2xl sm:text-3xl tracking-tight mb-4">
                  {s.name}
                </h2>
                <p className="font-serif font-light text-base lg:text-lg text-ink-dim leading-snug mb-7">
                  {s.outcome}
                </p>
                <ul className="mt-auto flex flex-col gap-3 pt-6 border-t border-line">
                  {s.includes.map((item) => (
                    <li key={item} className="flex gap-3 text-sm text-ink-dim leading-relaxed">
                      <span className="text-accent">→</span>
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* ─────────── Build advantage band ─────────── */}
      <section className="relative h-[340px] sm:h-[420px] lg:h-[500px] w-full border-y border-line">
        <ParallaxBackdrop
          src={images.laptopBeach}
          alt="Hand-coded foundations"
          strength={18}
          overlay={0.62}
          sizes="100vw"
          className="absolute inset-0"
        />
        <div
          aria-hidden
          className="absolute inset-x-0 top-0 h-28 z-10 pointer-events-none"
          style={{ background: 'linear-gradient(to bottom, #0a0a0b, transparent)' }}
        />
        <div
          aria-hidden
          className="absolute inset-x-0 bottom-0 h-28 z-10 pointer-events-none"
          style={{ background: 'linear-gradient(to top, #0a0a0b, transparent)' }}
        />
        <div className="relative z-20 h-full flex items-center justify-center px-6">
          <Reveal className="text-center max-w-[760px]">
            <span className="eyebrow-dark mx-auto">The Build Advantage</span>
            <p className="font-serif font-light text-3xl sm:text-4xl lg:text-5xl leading-[1.15] tracking-tight mt-6 text-ink">
              Built by hand. <em className="italic text-accent-bright">Ranked by Google.</em>
            </p>
            <p className="text-ink-dim text-sm sm:text-base leading-relaxed mt-5 max-w-[60ch] mx-auto">
              We hand-code every site on modern frameworks. Lean, semantic code loads in under two seconds and gives full control over SEO — the edge that drag-and-drop builders can’t match.
            </p>
          </Reveal>
        </div>
      </section>

      {/* ─────────── Tech stack ─────────── */}
      <section className="border-t border-line py-24 lg:py-32">
        <div className="max-w-site mx-auto px-5 sm:px-10 lg:px-20">
          <Reveal className="mb-12">
            <span className="eyebrow-dark">Our Stack</span>
            <h2 className="font-serif font-light text-3xl sm:text-4xl lg:text-5xl mt-6">
              Built on modern technology.
            </h2>
          </Reveal>
          <div className="flex flex-wrap gap-3">
            {[
              'Next.js',
              'React',
              'TypeScript',
              'Payload CMS',
              'Tailwind CSS',
              'Node.js',
              'MongoDB',
              'Vercel',
            ].map((tech) => (
              <span
                key={tech}
                className="border border-line text-ink-dim px-5 py-2.5 rounded-full text-sm hover:border-accent hover:text-accent transition-colors"
              >
                {tech}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* ─────────── CTA ─────────── */}
      <section className="relative overflow-hidden py-24 lg:py-36 text-center border-t border-line">
        <div
          aria-hidden
          className="pointer-events-none absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[100vmin] h-[100vmin]"
          style={{
            background:
              'radial-gradient(circle, rgba(125, 211, 252, 0.12) 0%, transparent 60%)',
          }}
        />
        <div className="max-w-2xl mx-auto px-5 relative z-10">
          <h2 className="font-serif font-light text-4xl sm:text-5xl lg:text-6xl mb-9">
            Ready to build technology that <em className="italic text-accent">actually works?</em>
          </h2>
          <Link
            href="/contact"
            className="inline-flex items-center gap-2.5 px-8 py-4 bg-accent text-bg rounded-full font-medium text-sm hover:bg-accent-bright transition-all hover:-translate-y-0.5 hover:shadow-[0_12px_32px_rgba(125,211,252,0.25)]"
          >
            Start a conversation <span aria-hidden>→</span>
          </Link>
        </div>
      </section>
    </div>
  )
}
